{
  "scripts": {
    "tsc": "bunx tsc --noEmit",
    "loc:all": "bun scripts/build-loc.ts --all --out dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@types/node": "^25.2.0"
  }
}